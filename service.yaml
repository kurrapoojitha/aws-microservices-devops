apiVersion: v1
kind: Service
metadata:
  name: devops-service
spec:
  type: NodePort
  selector:
    app: devops-app
    version: v1   # 🔥 Initially pointing to BLUE
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007
